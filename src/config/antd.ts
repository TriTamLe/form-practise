import { ThemeConfig } from 'antd'
import colorsConfig from './color'

export const themeConfig: ThemeConfig = {
  token: {
    colorPrimary: colorsConfig.DEEP_SPACE_SPARKLE,
    colorTextBase: colorsConfig.RICH_BLACK,
  },
}
